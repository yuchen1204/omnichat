# Copy to keystore.properties (ignored by Git) for a local signed release build.
# CI should supply the same values through environment variables.
KEYSTORE_PATH=path/to/release-key.jks
STORE_PASSWORD=replace-me
KEY_ALIAS=upload
KEY_PASSWORD=replace-me
