/**
 * docx-reader.js — Synchronous DOCX document parser placeholder.
 *
 * This is a Task 3 placeholder. Actual DOCX parsing logic will be implemented
 * in Task 5.
 *
 * Requirements:
 *  - Must define a synchronous parseDocument function
 *  - No Promise, async/await, Worker, DOM, Canvas, node:, require, process, or Buffer
 *  - Returns {format: "docx", text: string, warnings: string[]}
 */

/**
 * Parse a DOCX document's bytes and extract text content.
 * @param {{name: string, mimeType: string, bytes: Uint8Array}} input
 * @returns {{format: string, text: string, warnings: string[]}}
 */
function parseDocument(input) {
  // Placeholder: will be implemented in Task 5
  return result("docx", "", ["DOCX parser not yet implemented"]);
}