/**
 * pdf-reader.js — Synchronous PDF document parser placeholder.
 *
 * This is a Task 3 placeholder. Actual PDF parsing logic will be implemented
 * in Task 4.
 *
 * Requirements:
 *  - Must define a synchronous parseDocument function
 *  - No Promise, async/await, Worker, DOM, Canvas, node:, require, process, or Buffer
 *  - Returns {format: "pdf", text: string, warnings: string[]}
 */

/**
 * Parse a PDF document's bytes and extract text content.
 * @param {{name: string, mimeType: string, bytes: Uint8Array}} input
 * @returns {{format: string, text: string, warnings: string[]}}
 */
function parseDocument(input) {
  // Placeholder: will be implemented in Task 4
  return result("pdf", "", ["PDF parser not yet implemented"]);
}